name: Ethereum News Feed

on:
  schedule:
    - cron: "0 */3 * * *"  # Runs every 3 hours (≈5x/day)
  workflow_dispatch:

jobs:
  fetch-and-post:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version
